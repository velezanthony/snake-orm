"""SnakeORM's registry of compiled models."""

from snakeorm.registry.registry import SnakeRegistry as SnakeRegistry
from snakeorm.registry.registry import registry_of as registry_of
from snakeorm.registry.registry import registry as registry
