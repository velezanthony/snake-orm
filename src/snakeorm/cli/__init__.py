"""SnakeORM CLI: the `snakeorm` entry point (see [project.scripts]). Argparse only; it operates on the on-disk history through the loader and the runner."""

from snakeorm.cli.app import main as main
