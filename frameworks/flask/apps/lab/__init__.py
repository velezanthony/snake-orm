"""Query lab: pages that exercise DIFFERENT ORM capabilities so you can try them out while watching
the debug panel. Each view fires a group of operations (aggregates, subqueries, joins, pagination,
or the deliberate anti-patterns) and the SSR panel shows the SQL it generated.
"""
